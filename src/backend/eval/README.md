# Evaluation data

Ba file CSV được giữ lại làm dữ liệu kiểm thử hồi quy. Runner JavaScript cũ đã
được loại bỏ vì Agent và Tools không còn chạy ở Frontend; bộ kiểm thử mới nên gọi
trực tiếp FastAPI hoặc các service Python trong `backend/app/services`.
